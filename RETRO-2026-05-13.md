# Retrospective — /devloop · 2026-05-13

**Run**: `usage-loop-20260513`
**Working directory**: `/Users/antaresyuan/Downloads/personal_website`
**Tasks completed**: 3 (issues #151, #150, #152 → PRs #154, #155, #156)
**Friction events logged**: 4 (2 real friction, 2 `stop_condition_hit` = guardrails working)
**Sub-agent verdict**: 2 friction classes at count=1 — below threshold (3); no PR this cycle.

---

## A. Friction observed (this run)

| Class | Count | Examples | Hypothesized root cause |
|---|---|---|---|
| `retry::wrangler-syntax-v2-vs-v3` | 1 | `LOOP-FRICTION.jsonl` L2 — `kv:namespace` (colon, v2) written into README + issue comment; operator on wrangler v4 hit `Unknown arguments`. Patched docs mid-flight + pushed `f83a5f0`. | No skill step requires the loop to check the installed version of operator-facing CLIs before authoring docs. Wrangler is high-churn (v2→v3→v4 in ~2yrs with breaking subcommand renames). Model defaulted to training-cutoff syntax. |
| `subagent_flag_persistent::heatmap-edge-weekday` | 1 | L3 — PR #156 heatmap math silently dropped ~2 oldest cells when today=Sunday. Local tests only covered today=Thu. Sub-agent caught 4 bugs incl. this; all fixed pre-merge in `3f774e9`. | §2 step 4 doesn't call out test classes where weekday/timezone/edge-of-grid matters. Default behavior: pick one representative test case, ship. Calendar/grid math needs all-7 weekday coverage. |
| `stop_condition_hit::designed-pause` | 2 | L1 (wrangler-bind handoff) + L4 (queue exhausted). | **Not friction** — loop stopping cleanly. Excluded from accumulators per skill §6 + §2 STOP conditions. |

## B. Quality observations

Sub-agent review pulled real weight:

- **PR #156** — 4 issues caught, 0 reached `main`: heatmap Sunday-edge math (would have shipped a visibly broken calendar 1 day in 7), SSR layout shift on first fetch, missing fetch timeout (hung-tab risk on Worker outage), and spammy `aria-live` on 60s/7s cadences (a11y noise).
- **PR #155** — 3 operational hardenings: install.sh chmod, version-manager Node guard (nvm/asdf/fnm/volta), macOS-only guard.
- **PR #154** — 1 wording fix (misleading "different sources never collide" comment) — minor credibility-cost-avoidance.

Operator step-ins: **one substantive** (the wrangler syntax fix had to be ack'd and the loop pushed a follow-up commit). The wrangler-bind pause was designed. All other merges proceeded without intervention.

**Verdict**: §2.9 independent review is load-bearing for frontend math + ops hardening. No quality regressions reached the live site.

## C. Pacing

`ScheduleWakeup` barely engaged — the operator was awake and interactive across all 3.5h. The loop hand-rolled rather than self-paced. **This is a divergence from the skill's design intent** (overnight unattended). No `pacing_mismatch` events logged, but also no validation of the §3 defaults (60s / 150s / 1200s) against the personal_website Pages-build duration.

For the next overnight run, instrument actual Pages deploy time (push → marker-visible). If >150s the §2 step 13 default will undershoot.

## D. Proposed SKILL.md edits

### Diff #1 — Operator-facing CLI commands: version-check before writing docs

**Triggered by**: `retry::wrangler-syntax-v2-vs-v3` (count=1, threshold=3 — **not yet at threshold**)

```diff
--- a/SKILL.md (§2 step 4 "Build & test hard")
+++ b/SKILL.md (proposed)
@@ step 4 @@
 4. **Build & test hard.** Run the build — it must pass AND be idempotent (run it twice; no diff on
    the second run). `node -c` (or the equivalent lint) any new/changed source files. Grep the built
    output for the markup/strings you expect. Run the test suite if there is one. Goal: "force no
    problems" — don't rush.
+
+   **Operator-facing CLI commands in docs/READMEs/PR bodies:** before committing instructions that
+   the operator will paste into a shell, check the *installed* version of any high-churn CLI
+   (`wrangler --version`, `gh --version`, `vercel --version`, `npm --version`) and match the
+   syntax to *that* version, not training defaults. Wrangler v2 → v3 renamed `kv:namespace` to
+   `kv namespace` (space). `gh` reorganises subcommands across majors. Don't ship operator
+   instructions blind.
```

**Status**: Park. Re-evaluate at next /iterate if class accumulates 2 more events.

**Anti-pattern check**: PASS (additive guidance; no deny/safety/red-line touched).

### Diff #2 — Calendar/grid math: test all 7 weekdays

**Triggered by**: `subagent_flag_persistent::heatmap-edge-weekday` (count=1, threshold=3 — **not yet at threshold**)

```diff
--- a/SKILL.md (§2 step 4 "Build & test hard")
+++ b/SKILL.md (proposed)
@@ step 4 @@
+
+   **Calendar/grid/time-bucketed math:** when a task involves a date-grid (heatmap, streak,
+   week-view, month-view) or any "today-relative" bucketing, exercise *every* weekday — not just
+   today's. A heatmap that works on Thursday can silently drop oldest cells on Sunday because the
+   week-start offset shifts. Build a tiny test harness that loops `[Sun..Sat]` and asserts cell
+   count / oldest-date / newest-date invariants.
```

**Status**: Park. High-impact class — this bug would have shipped to live with only §2.9 sub-agent review as the safety net. Consider promoting E1 (below) to skill body now even without a PR.

**Anti-pattern check**: PASS.

### Diff #3 — Mode-aware pacing note (cosmetic)

**Triggered by**: §C observation; no friction count.

```diff
--- a/SKILL.md (§3 Pacing)
+++ b/SKILL.md (proposed)
@@ §3 @@
 ScheduleWakeup is how you span turns. ~60s for "just keep working"; ~150s for "waiting on a deploy";
 longer (1200s+) only if there's genuinely nothing to check. The harness re-invokes you when it fires.
 If multiple wakeups queue up and a prompt's "STATE:" looks stale, ALWAYS `git -C <repo> log` at the
 top of the turn and reason from the real state.
+
+ **Interactive vs overnight mode.** If the operator is awake and reacting in the same session,
+ ScheduleWakeup mostly idles — that's fine, just don't *force* delays for their own sake. The
+ defaults above are calibrated for unattended runs; in interactive mode prefer continuing
+ immediately when the previous step's output is in hand.
```

**Status**: Park. Cosmetic, no count.

## E. Cross-cutting lessons

- **E1**. Calendar/grid/today-relative math: test all 7 weekdays before PR; one weekday is not coverage.
- **E2**. Before shipping operator-facing CLI snippets, run `<tool> --version` and match syntax to the installed major.
- **E3**. The independent sub-agent review (§2.9) is load-bearing for frontend math bugs — never skip it to "save a turn." (4 bugs caught on #156, 3 on #155, 1 on #154.)
- **E4**. Designed pauses (operator-side dashboard/credential work) are not friction — log as `stop_condition_hit`, not `retry`.

## F. PR-eligible diffs summary

| Friction class | Cumulative count | Threshold | PR | Status |
|---|---|---|---|---|
| `retry::wrangler-syntax-v2-vs-v3` | 1 | 3 | — | wait 2 more occurrences (park D1) |
| `subagent_flag_persistent::heatmap-edge-weekday` | 1 | 3 | — | wait 2 more occurrences (park D2) — but E1 worth promoting now |
| `stop_condition_hit::designed-pause` | 2 | n/a | — | guardrail working, not friction |

**Recommendation**: No PR to `AntaresYuan/claude-devloop` this cycle. State file initialized; the next `/iterate` invocation will pick up if either class crosses threshold. The operator can choose to hand-apply E1/E2 as additive notes now (low risk) without going through the PR flow.
